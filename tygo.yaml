# Generates web/src/lib/model.gen.ts from internal/model - the wire
# contract between the Go snapshot and the SPA. Regenerate with `make types`.
packages:
  - path: "github.com/epheo/stornas/internal/model"
    output_path: "web/src/lib/model.gen.ts"
